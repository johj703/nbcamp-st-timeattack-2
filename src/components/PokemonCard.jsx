const PokemonCard = () => {
  return (
    <div>
      <img src={""} alt={""} />
      <p>{PokemonCard.korean_name}</p>
      <button>삭제</button>
      <button>추가</button>
    </div>
  );
};

export default PokemonCard;
